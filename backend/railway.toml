[build]
buildCommand = "npm install --include=dev && npx prisma generate && ./node_modules/.bin/tsc -p tsconfig.build.json && ls -la dist"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main"